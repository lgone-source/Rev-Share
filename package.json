{
  "name": "rev-share-bot",
  "version": "1.0.0",
  "description": "Slack bot for on-demand per-partner rev share breakdowns",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/app.ts",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.22.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
