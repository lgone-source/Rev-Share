# Slack App credentials — get these from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_APP_TOKEN=xapp-your-app-token   # Required for Socket Mode (local dev)

# CAT pricing tool integration — fill in once access is confirmed
CAT_API_URL=
CAT_API_KEY=

# Server port (default 3000)
PORT=3000
